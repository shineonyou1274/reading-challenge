# 👑 황실 기록소 (Royal Archives) - 공식 편람

## 1. 앱 소개 (Introduction)
**황실 기록소(Royal Archives)**는 독서를 단순한 행위가 아닌, 지혜를 쌓고 성취를 증명하는 **장엄한 여정(Quest)**으로 재해석한 게이미피케이션 독서 기록 앱입니다.

사용자는 황실의 학자가 되어 독서를 통해 **XP(경험치)**와 **골드**를 획득하고, 입문 학자에서 대현자까지 신분을 상승시킬 수 있습니다.

### ✨ 핵심 철학
*   **몰입(Immersion):** 숲속의 황실 서고에 들어온 듯한 몽환적인 다크/네온 그린 디자인.
*   **성취(Achievement):** 독서 시간만큼 보상을 받고 레벨업하는 RPG 시스템.
*   **연결(Connection):** 다른 학자들의 독서 현황을 실시간으로 확인하는 커뮤니티.

---

## 2. 주요 기능 (Features)

### 🏠 대시보드 (Quest Board)
*   **타이머 & 기록:** 독서 시간을 측정하고 `중단/저장` 할 수 있습니다.
*   **레벨 시스템:** 현재 레벨, XP 진행도, 연속 독서일(Streak)을 시각적으로 확인합니다.
*   **목표 관리:** 오늘의 독서 목표 달성 현황을 보여줍니다.

### 📚 왕실 서고 (Royal Library)
*   **비주얼 서가:** 읽은 책들이 책등(Spine)의 높낮이가 다른 시각적 서가 형태로 쌓입니다.
*   **기록 보관:** 과거의 독서 기록, 감상평(메모), 획득한 보상을 날짜순으로 열람합니다.

### 👥 학자들의 광장 (Community)
*   **실시간 전령:** 다른 유저들이 방금 어떤 책을 읽고 기록했는지 실시간 피드로 확인합니다.
*   **제국 랭킹:** 가장 많은 XP를 쌓은 학자들의 순위를 보여줍니다.

### 👤 학자 프로필 (Profile)
*   나만의 아바타, 누적 골드, 전체 랭킹, 현재 신분을 확인하고 로그아웃 할 수 있습니다.

---

## 3. 책 표지 시스템 (Book Cover System)

현재 시스템은 데모 이미지를 사용 중이며, 실제 책 표지를 가져오기 위해서는 **API 연동**이 필요합니다.

### 🛠️ 실제 구현 방식 (기술 가이드)
한국 도서 데이터베이스를 가져오기 위해 **네이버 개발자 센터(Naver Developers)**의 API를 사용합니다.

1.  **API 키 발급:**
    *   [네이버 개발자 센터](https://developers.naver.com/) 접속 -> 애플리케이션 등록 (검색 API).
    *   `Client ID`와 `Client Secret`을 발급받습니다.

2.  **환경 변수 설정 (.env):**
    프로젝트 폴더의 `.env` 파일에 발급받은 키를 입력합니다.
    ```env
    VITE_NAVER_CLIENT_ID=여기에_아이디
    VITE_NAVER_CLIENT_SECRET=여기에_시크릿
    ```

3.  **작동 원리:**
    *   사용자가 책 제목을 입력하고 돋보기 버튼을 누릅니다.
    *   앱이 네이버 서버에 요청을 보냅니다 (`/v1/search/book.json`).
    *   받아온 데이터 중 `image` 필드(표지 URL)를 화면에 띄웁니다.

---

## 4. 설치 및 실행 (Setup Guide)

### 필수 요구사항
*   Node.js (v16 이상)
*   Firebase 프로젝트 (필수)

### 실행 방법
1.  **의존성 설치:** 터미널에 `npm install` 입력.
2.  **환경 변수 설정:** `.env` 파일 생성 후 Firebase 및 Naver API 키 입력.
3.  **앱 실행:** `npm run dev` 입력 후 브라우저 접속.

---

_Written by Royal Archivist AI_
