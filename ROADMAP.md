# Royal Reading Quest - 장기 로드맵

## 4. 오프라인 모드 및 PWA 지원 확대

### 목표
인터넷이 끊긴 환경(지하철, 비행기 등)에서도 타이머와 기록이 정상 작동하도록 한다.

### 구현 방향
- Firestore `enablePersistence()` 활성화로 기본 오프라인 캐시 지원
- 독서 타이머 세션을 IndexedDB(또는 localStorage)에 로컬 저장
- 온라인 복구 시 미동기화 세션을 Firestore에 batch write로 업로드
- 오프라인 상태 표시 UI (배너 또는 아이콘)
- Service Worker 캐싱 전략 최적화 (현재 generateSW → 커스텀 전략 검토)

### 난이도: 높음
- 오프라인/온라인 전환 시 충돌 해결 로직 필요
- 타이머 세션 중 네트워크 전환 시 데이터 무결성 보장

---

## 5. 연말 리포트 (스페셜 리포트 + 바이럴 공유)

### 목표
사용자의 1년 독서 데이터를 시각적으로 요약하여 공유 가능한 리포트를 생성한다.

### 구현 방향
- 연간 통계 집계: 총 독서 시간, 완독 권수, 최다 독서 장르, 최장 연속 기록
- "가장 몰입했던 순간" (최장 세션 + 해당 책 정보)
- "올해의 책" (가장 오래 읽은 책)
- Canvas 또는 html2canvas로 공유용 이미지 생성
- 인스타 스토리 / 카카오톡 공유 연동
- 시기: 매년 12월 중순 활성화

### 난이도: 중간
- 데이터는 이미 충분히 쌓이고 있음 (sessions 컬렉션)
- 이미지 생성 + 공유 API 연동이 핵심 작업
